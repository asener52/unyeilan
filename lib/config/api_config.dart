class ApiConfig {
  static const String baseUrl = 'https://duyuru.unye.bel.tr/api';
  static const String serverBase = 'https://duyuru.unye.bel.tr';

  static const Duration connectTimeout = Duration(seconds: 15);
  static const Duration receiveTimeout = Duration(seconds: 30);
}
